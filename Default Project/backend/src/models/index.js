const User = require('./User');
const Booking = require('./Booking');
const { GradingSession, GradingResult } = require('./Grading');
const Device = require('./Device');

module.exports = {
  User,
  Booking,
  GradingSession,
  GradingResult,
  Device,
};